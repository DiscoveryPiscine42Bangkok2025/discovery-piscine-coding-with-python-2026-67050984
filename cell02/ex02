#!/usr/bin/env python3

# กำหนดรหัสผ่านที่ถูกต้อง (ตามโจทย์เป๊ะๆ)
password = "Python is awesome"

# รับค่ารหัสผ่านจากผู้ใช้
user_input = input()

# เช็คว่าตรงมั้ย
if user_input == password:
    print("ACCESS GRANTED")
else:
    print("ACCESS DENIED")