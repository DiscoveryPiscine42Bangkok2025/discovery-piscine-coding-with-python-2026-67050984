#!/usr/bin/env python3
import sys

# เช็คว่ามีการใส่ Argument พ่วงท้ายมาตอนรันโปรแกรมหรือไม่
# (len(sys.argv) ปกติจะเป็น 1 ถ้ารันแค่ชื่อไฟล์)
if len(sys.argv) > 1:
    print("none")
else:
    # ลูปที่ 1: รันแม่สูตรคูณจาก 0 ถึง 10
    i = 0
    while i <= 10:
        # พิมพ์หัวข้อแม่สูตรคูณ (end="" คือสั่งให้ไม่ขึ้นบรรทัดใหม่)
        print("Table de " + str(i) + ":", end="")
        
        # ลูปที่ 2: รันตัวเลขที่เอามาคูณจาก 0 ถึง 10
        j = 0
        while j <= 10:
            # พิมพ์ช่องว่างตามด้วยผลคูณ (end="" เพื่อให้อยู่บรรทัดเดียวกัน)
            print(" " + str(i * j), end="")
            j += 1
            
        # พอจบแม่สูตรคูณ 1 แม่ ให้ขึ้นบรรทัดใหม่ 1 ที
        print()
        i += 1